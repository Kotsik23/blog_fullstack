// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         Int       @id @default(autoincrement())
  createdAt  DateTime  @default(now()) @map("created_at")
  updatedAt  DateTime  @updatedAt @map("updated_at")
  email      String    @unique
  password   String
  rtHash     String?
  roles      Role[]    @default([USER])
  posts      Post[]    @relation("author")
  likedPosts Post[]
  comments   Comment[]
}

model Post {
  id        Int       @id @default(autoincrement())
  createdAt DateTime  @default(now()) @map("created_at")
  updatedAt DateTime  @updatedAt @map("updated_at")
  title     String
  content   String
  imageUrl  String    @default("https://firebasestorage.googleapis.com/v0/b/blogging-a1f96.appspot.com/o/images%2Fe92e773d-aae7-45ab-b0cf-73e640eef51a?alt=media&token=db8a8b9c-c088-4fc6-b3d4-f2daa2c345a9") @map("image_url")
  author    User      @relation("author", fields: [userId], references: [id])
  userId    Int
  likes     User[]
  comments  Comment[]
}

model Comment {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now()) @map("created_at")
  body      String
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
  post      Post     @relation(fields: [postId], references: [id])
  postId    Int
}

enum Role {
  USER
  ADMIN
  MANAGER
}
